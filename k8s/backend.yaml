apiVersion: apps/v1
kind: Deployment
metadata:
  name: flatbuddy-backend
  namespace: "2401066"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flatbuddy-backend
  template:
    metadata:
      labels:
        app: flatbuddy-backend
    spec:
      imagePullSecrets:
        - name: nexus-secret
      containers:
        - name: flatbuddy-backend
          image: nexus-service-for-docker-hosted-registry.nexus.svc.cluster.local:8085/my-repository2401066/flatbuddy-backend:latest
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
---
apiVersion: v1
kind: Service
metadata:
  name: flatbuddy-backend
  namespace: "2401066"
spec:
  selector:
    app: flatbuddy-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
